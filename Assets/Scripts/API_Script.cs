using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class API_Script : MonoBehaviour
{
    /*[SerializeField]
    Text testText;

    private const string url = "www.google.com";

    public void Request()
    {
         request = new WWW(url);

        StartCoroutine(WaitForResponse(request));
    }

    [System.Obsolete]
    private IEnumerator WaitForResponse(WWW req)
    {
        yield return req;

        testText.text = req.text;
    }*/
    
}
